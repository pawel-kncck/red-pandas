check requirements.txt --> do we need all thos packages?
move to pip-tool for dependencies management (requirenets.in + requirenets.txt) + separate dev requirements
Fix UI

- Dark mode

Add authentication
Add docker
and docker-dev
